# URL pública do site — usada em metadata, sitemap e robots.txt
# Lida em BUILD time (statically generated em `next build`), não em runtime.
# Copie para .env.local (ou configure na plataforma de deploy, ex.: Netlify)
NEXT_PUBLIC_SITE_URL=https://image-compressor-web.netlify.app
